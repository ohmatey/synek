// Shared look for the canvas's floating control chips — each piece of chrome
// floats independently over the canvas (no grouping bar), frosted so it stays
// legible over nodes and the dot grid.
export const floatChip = 'rounded-lg border border-border bg-card/80 shadow-sm backdrop-blur-md'
