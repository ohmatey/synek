// Shared left-rail tab-trigger styling for the vertical-tabs settings shells
// (account SettingsDialog + timeline CanvasSettings), so the two dialogs match.
// A leaf module with no component deps — safe to import from any bundle.
export const VTAB =
  'h-9 w-full flex-none justify-start gap-2 rounded-md px-2.5 text-sm data-[state=active]:bg-card data-[state=active]:shadow-sm'
